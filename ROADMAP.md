# 小黑猫项目方案地图

> 版本：2026-08-31　状态：M0 已完成，M1 开工
> 本文档回答三个问题：往哪走（技术路线）、长什么样（体验）、怎么走（里程碑）。

---

## 1. 一句话定位

**跨 AI 工具的思维整理面板是本体，桌面小猫是它的陪伴和入口。**

AI 助手（Claude Code / Codex / Gemini / DSH）在对话中产生的想法、结论、方案、疑问，实时沉淀到本地磁盘的四层骨架；桌面上的小黑猫负责提醒你、给你看。

## 2. 目标体验（北极星指标）

| 维度 | 现在（Electron） | 目标 |
|---|---|---|
| 常驻内存 | ~350MB | **<100MB** |
| 安装包 | ~200MB | **<30MB** |
| 用户安装 | 装 Node + clone + npm install | **下载双击即用** |
| 面板 | 点猫 → 圆环 → 气泡面板 | **不变**（体验一行不改） |
| 数据 | 本地 `~/.mind-board` | 不变，且回家/换机可同步代码继续开发 |

体验红线：**思维面板的视觉效果和交互不变**；猫能拖、能点、贴边、置顶、点击穿透。

## 3. 技术路线：四层解耦，各层可独立更换

```
┌─ 壳层（正在换）：Electron → C# 原生（Win）→ Tauri（跨平台备用）
│    只负责：开窗、透明、置顶、穿透、托盘、热键
├─ 界面层（不动）：pet / dock / bubble / dashboard / settings 网页
│    原则：简单图形用原生画（猫、圆环），复杂排版用网页（面板）
├─ 服务层（不动）：src/server/app.mjs，127.0.0.1:13134，零依赖
├─ 数据层（不动）：src/core/store.mjs —— 原子写盘 / 去重 / 目标熔断 / 新旧替代
└─ 接入层（不动）：MCP（任意 harness）、Claude Code 钩子、DSH 插件
```

换壳只动最上面一层——这是整个方案的地基：**值钱的东西（数据、界面、接入）永远在壳之外**。

## 4. 设计原则（已敲定的取舍）

1. **该用网页的才用网页**：猫和圆环是简单图形 → 原生画（Win32 分层窗口，桌宠品类二十年标准做法）；面板是排版密集型 → 网页，但**按需出场**（点开才来，关掉释放内存）
2. **不用即停**（抄 Lively Wallpaper）：隐藏/被覆盖时停渲染，CPU 归零
3. **每像素透明命中**：分层窗口天然支持（半透明光晕不拦截鼠标），不再需要 Electron 那套形态学膨胀迂回
4. **幽灵可见类 bug 整类消失**：原生窗口像素直接交给 DWM，没有浏览器合成器这个中间商
5. **数据安全已加固**（M0）：id 白名单防穿越、反跨站投毒、写失败上抛、损坏备份

## 5. 里程碑

| 阶段 | 内容 | 验收 |
|---|---|---|
| **M0 地基** ✅ | git 入库、安全修复包、防幽灵可见、推送 GitHub | 已完成 2026-08-31 |
| **M1 猫窗原型** | C# 分层窗口：显示猫、呼吸、拖拽、逐像素穿透、贴边、右键菜单 | 常驻 <40MB；手感不输 Electron 版 |
| **M2 圆环+面板** | dock 圆环原生画；面板 WebView2 按需窗（播现有网页） | 面板首开 <1s；关掉后内存回落 |
| **M3 完整替换** | 托盘、热键、设置页、开机自启；冻结并删除 Electron | 日常主力使用一周无回归 |
| **M4 跨平台**（按需启动） | Tauri 壳出 Mac/Linux 版，网页与数据层原样复用 | 需要时再做，不提前投入 |

并行线索：README/安装包打磨、测试补齐（HTTP 安全、并发写回归）。

## 6. 风险与对策

| 风险 | 对策 |
|---|---|
| WebView2 面板重演幽灵可见 | M2 第一天验证；Electron 版防御经验可移植（WebView2 支持注入同款启动参数） |
| C# 穿透手感不及预期 | M1 先验证手感再投入后续；不行退回 Tauri（网页资产无损） |
| 双壳并存期混乱 | Electron 即刻冻结，不再加功能 |
| 全屏游戏盖住置顶窗 | Windows 系统限制，所有方案一样，不列为 bug |

## 7. 回家继续开发指南

```bash
git clone https://github.com/dvlinzh/xiaoheimao.git
# github.com 直连不通时，git 命令加：-c http.proxy=http://127.0.0.1:7890（端口以本机代理为准）
```

- 数据层/服务/接入层：纯 Node 零依赖，`node src/server/app.mjs` 即起
- C# 壳：`shell-win/` 目录，Windows 自带 csc.exe 编译（见该目录 README）
- 思维板数据在 `~/.mind-board/`（不入库，各机器各自积累）
